import { schema } from '@getnextbike/db';

export const locationKinds = schema.resellerLocationKind.enumValues;
export const locationStatuses = schema.resellerLocationStatus.enumValues;
export const robotsPolicies = schema.robotsPolicy.enumValues;
